#include <stdio.h>
#include <stdlib.h>
#include <unist.h>
#include <math.h>

float x,y,z;
int r,g,b;

FILE *ifp;
char *mode = "rt";

while (fscanf(ifp, "%f;%f;%f;%d;%d;%d", &x, &y, &z, &r, &g, &b) != EOF) {

}
